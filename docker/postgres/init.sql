-- Enable UUID extension
CREATE EXTENSION IF NOT EXISTS "pgcrypto";

-- Create initial database structure (migrations will handle the rest)
-- This file can be left mostly empty as Laravel migrations will create tables
